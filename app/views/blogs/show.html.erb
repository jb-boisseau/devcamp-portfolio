<div class="col-sm-8 blog-main">
  <h1><%= @blog.title %></h1>
  <p><%= @blog.body %></p>
  <%= link_to 'Edit', edit_blog_path(@blog), class:"btn btn-primary mr-2 shadow" if logged_in?(:site_admin) %>
</div>
